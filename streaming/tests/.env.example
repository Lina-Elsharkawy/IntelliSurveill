# Test Configuration
# Copy this file to .env and adjust values for your environment

# Kafka Configuration (use external port for tests running outside Docker)
KAFKA_BOOTSTRAP_SERVERS=localhost:9093

# PostgreSQL Configuration (use external port for tests running outside Docker)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=logging_db
POSTGRES_USER=mohamed
POSTGRES_PASSWORD=mohamed
