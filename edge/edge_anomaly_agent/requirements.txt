# Jetson Nano / JetPack 4.6.1 / Python 3.6
# Keep JetPack's system OpenCV with CUDA/DNN. Do NOT pip-install opencv-python here.

numpy==1.19.5
pyyaml==6.0.2
requests==2.32.3
kafka-python==2.0.2
Pillow==8.4.0

# Install torch separately from NVIDIA Jetson wheels that match JetPack.
# Do not add opencv-python here, because we want the Jetson OpenCV build.